# Software to display lights on a screen

## General

Reads the serial port of the Arduino through USB, and reacts to logged events by updating lights on a Canvas.

## Installation

1. Install tkinter (OS/distribution-specific)
2. `pip install pyserial`
